import { Route } from '@angular/router';

import { RealtimeStatisticComponent } from './index';

export const RealtimeStatisticRoutes: Route[] = [
	{
		path: 'realtime-statistic',
		component: RealtimeStatisticComponent
	}
];
