import { Route } from '@angular/router';

import { CameraSettingsComponent } from './index';

export const CameraSettingsRoutes: Route[] = [
	{
		path: 'camera-settings',
		component: CameraSettingsComponent
	}
];
