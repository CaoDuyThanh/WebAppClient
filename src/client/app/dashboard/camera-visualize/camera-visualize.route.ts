import { Route } from '@angular/router';

import { CameraVisualizeComponent } from './index';

export const CameraVisualizeRoutes: Route[] = [
	{
		path: 'map-simulation',
		component: CameraVisualizeComponent
	}
];
