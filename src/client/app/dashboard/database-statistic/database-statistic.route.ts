import { Route } from '@angular/router';

import { DatabaseStatisticComponent } from './index';

export const DatabaseStatisticRoutes: Route[] = [
	{
		path: 'database-statistic',
		component: DatabaseStatisticComponent
	}
];

